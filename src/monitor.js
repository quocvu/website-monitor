/*
- check a site
- if success & error_mode => send 'back online' sms
- if fail => send & error_count < max => send failure sms and incr(error_count)
*/

'use strict';

var exports = module.exports = {
  var handler = function () {
    console.log('here');
  }
};